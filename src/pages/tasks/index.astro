---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { issues } from "../../data/issues";
import { Card } from "../../components";
console.log(issues);
---

<BaseLayout header="Tasks">
    {issues?.map(({ number, title, state }) => <Card title={title} number={number} state={state} />)}
</BaseLayout>
